// Simple implementation of reportWebVitals without dependencies
// This is a placeholder function that doesn't actually report anything

const reportWebVitals = (onPerfEntry) => {
  // In a real application, this would use the web-vitals package to report performance metrics
  // For now, we're just providing a placeholder to satisfy the import in index.js
  console.log('Performance reporting is disabled');
};

export default reportWebVitals;
